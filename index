<!DOCTYPE html>
<html lang="en">
<head>
<meta charset="UTF-8">
<title>For Margi 🐾❤️</title>
<link href="https://fonts.googleapis.com/css2?family=Playfair+Display:wght@500&family=Poppins:wght@300;500&display=swap" rel="stylesheet">

<style>
body {
  margin: 0;
  height: 100vh;
  display: flex;
  justify-content: center;
  align-items: center;
  background: linear-gradient(135deg, #fce4ec, #f8bbd0);
  font-family: 'Poppins', sans-serif;
}

.container {
  text-align: center;
  background: rgba(255, 255, 255, 0.9);
  padding: 60px;
  border-radius: 25px;
  box-shadow: 0 15px 40px rgba(0,0,0,0.15);
  width: 420px;
  position: relative;
}

h1 {
  font-family: 'Playfair Display', serif;
  color: #d81b60;
}

p { color: #444; }

button {
  padding: 12px 28px;
  font-size: 16px;
  border-radius: 25px;
  border: none;
  cursor: pointer;
  margin: 10px;
  transition: 0.3s;
}

button:hover { transform: scale(1.08); }

#yesBtn { background: #d81b60; color: white; }
#noBtn { background: #333; color: white; position: absolute; }

input[type="range"] { width: 80%; }
</style>
</head>

<body>

<div class="container" id="content"></div>

<script>
let step = 0;

function nextStep() {
  step++;
  render();
}

function moveButton() {
  const btn = document.getElementById("noBtn");
  const x = Math.random() * 200 - 100;
  const y = Math.random() * 200 - 100;
  btn.style.transform = `translate(${x}px, ${y}px)`;
}

function render() {
  const content = document.getElementById("content");

  // STEP 1
  if (step === 0) {
    content.innerHTML = `
      <h1>Margi (Vaydii) 🐱</h1>
      <p>Will you be my Valentine?</p>
      <button id="yesBtn" onclick="nextStep()">YES 💖</button>
      <button id="noBtn" onmouseover="moveButton()">NO 🙀</button>
    `;
  }

  // STEP 2
  else if (step === 1) {
    content.innerHTML = `
      <h1>Important Question 😌</h1>
      <p>Do you want to go out to have spicy momos?</p>
      <button onclick="nextStep()">YES 🌶️</button>
      <button onclick="nextStep()">YES (obviously)</button>
    `;
  }

  // STEP 3
  else if (step === 2) {
    content.innerHTML = `
      <h1>How much do you like me? 😌</h1>
      <p>Select between 5–10</p>

      <input type="range" min="5" max="10" value="5" id="likeScale"
        oninput="document.getElementById('likeValue').innerText = this.value">

      <p id="likeValue">5</p>

      <button onclick="submitLike()">Submit</button>

      <p id="warning" style="color:#d81b60;"></p>
    `;
  }

  // STEP 4
  else if (step === 3) {
    content.innerHTML = `
      <h1>How beautiful do you think you are?</h1>
      <p>Select from 1–10</p>

      <input type="range" min="1" max="10" value="5" id="beautyScale"
        oninput="document.getElementById('beautyValue').innerText = this.value">

      <p id="beautyValue">5</p>

      <button onclick="submitBeauty()">Submit</button>

      <p id="beautyMsg" style="margin-top:20px;color:#d81b60;"></p>
    `;
  }

  // FINAL STEP
  else {
    content.innerHTML = `
      <h1>Happy Valentine’s Day ❤️</h1>
      <p>
      I love you,<br>
      Now get ready… we’re getting those momos in future 🌶️🐾
      </p>

      <img src="us.jpg"
        style="margin-top:25px;width:280px;border-radius:20px;
        box-shadow:0 12px 30px rgba(0,0,0,0.25);transition:0.4s;"
        onmouseover="this.style.transform='scale(1.05)'"
        onmouseout="this.style.transform='scale(1)'">

      <p style="margin-top:15px;color:#d81b60;font-weight:500;">
      My favourite memory with you 🐾
      </p>
    `;
  }
}

function submitLike() {
  const value = document.getElementById("likeScale").value;

  if (value == 10) {
    nextStep();
  } else {
    document.getElementById("warning").innerText =
      "Hmm… that doesn’t seem right. Try again 😌";
  }
}

function submitBeauty() {
  document.getElementById("beautyMsg").innerText =
    "Wrong answer. There is no scale to define your beauty ✨";

  setTimeout(() => {
    document.getElementById("beautyMsg").innerHTML +=
      "<br><br><button onclick='nextStep()'>Continue ❤️</button>";
  }, 800);
}

render();
</script>

</body>
</html>
